---
services:
  node:
    image: ghcr.io/ripplefcl/distomon:latest
    ports:
      - 8010:8000
    environment:
      CONFIG_FILE_NAME: nodeconf.yml
    volumes:
      - ./conf:/app/src/config
    restart: unless-stopped
